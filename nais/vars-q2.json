{
  "_comment": "Every REPLACE_ME below is a value only the team can supply — see docs/PREREQUISITES.md. The deploy workflow refuses to run while any of them survives, so a half-filled file fails loudly instead of deploying something wrong.",

  "app_name": "melosys-muninn-q2",
  "namespace": "REPLACE_ME_namespace",
  "team": "REPLACE_ME_team",

  "_comment_ingresses": "TWO hostnames, both full https:// URLs. Both feed spec.ingresses, and MUNINN_ALLOWED_ORIGINS is DERIVED from the pair in app.yaml — there is deliberately no third variable to keep in step. intern = naisdevice; ansatt = reachable for team members without developer access.",
  "ingress_intern": "REPLACE_ME_https_ingress_intern_url",
  "ingress_ansatt": "REPLACE_ME_https_ingress_ansatt_url",

  "tenant": "REPLACE_ME_entra_tenant",
  "group_muninn_bruker": "REPLACE_ME_group_object_id",

  "_comment_admins": "There is deliberately no `admin_oids` key: an oid is a stable personal identifier for a named human, and this repo is public. MUNINN_ADMIN_IDENTS comes from the nais secret named below, which somebody creates in the namespace out of band — see docs/PREREQUISITES.md §10. The secret's KEY must be literally MUNINN_ADMIN_IDENTS; envFrom injects keys verbatim.",
  "admin_secret": "melosys-muninn-q2-admin-idents",

  "_comment_vertex": "There is deliberately no `vertex_host` key. The egress host is DERIVED by the deploy workflow from gcp_project + vertex_region and passed to nais/deploy as a VAR — leave it out and it stays that way. Adding it back means either a REPLACE_ME the first run hard-fails on, or a hand-filled value the computed VAR silently overrides.",
  "gcp_project": "REPLACE_ME_gcp_project_id",
  "vertex_region": "REPLACE_ME_vertex_region",

  "db_instance": "muninn-db",
  "db_name": "muninn",
  "db_tier": "db-custom-1-3840"
}
